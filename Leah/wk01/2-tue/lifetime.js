var currentAge = 27;

var maxAge = 100;

var dailyEst = 12;

var totalDumplingsLeft = (maxAge - currentAge) * (dailyEst * 365);

console.log("You will need " + totalDumplingsLeft + " dumplings to last you until the ripe old age of " + maxAge + ".");
